apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: notesapp
spec:
  # แก้ไขเป็น ClusterIP 
  type: ClusterIP 
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80