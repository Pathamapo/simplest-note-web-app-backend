apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: notesapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend      
        image: YOUR_FRONTEND_IMAGE
        ports:
        - containerPort: 80  